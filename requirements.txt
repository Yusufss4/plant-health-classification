# Core deep learning framework
# Note: EfficientNet-B0 requires torchvision >= 0.13.0
# Note: DINOv3 ViT-S/14 is loaded from timm library (PyTorch Image Models)
torch>=2.0.0
torchvision>=0.16.0
timm>=1.0.20

# Data manipulation and numerical computations
numpy>=1.24.0

# Image processing
Pillow>=9.5.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Evaluation metrics
scikit-learn>=1.2.0

# Progress bars
tqdm>=4.65.0

# Data preparation (for automatic dataset download)
# Pin protobuf <7: TFDS 4.9.x reads field.label on protobuf descriptors; protobuf 7 uses
# UPB descriptors without .label (AttributeError in dataset_info.read_from_directory).
# TensorFlow 2.21+ requires protobuf>=6.31.1,<8.
protobuf>=6.31.1,<7
tensorflow>=2.13.0
tensorflow-datasets>=4.9.0

# Optional: For Jupyter notebook support
# jupyter>=1.0.0
# ipykernel>=6.22.0

# ONNX export and C++ parity (export_mobilenet_onnx.py)
onnx>=1.13.0
onnxruntime>=1.14.0
